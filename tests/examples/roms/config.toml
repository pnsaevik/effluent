# Characteristics of the pipe and the effluent flow
pipe.csv.file = "pipe.csv"

# Characteristics of the ambient water masses
[ambient.roms]
file = "../../forcing_*.nc"
latitude = 59.03
longitude = 5.68
azimuth = 45

# Output options
[output]
csv.file = "out.csv"
trajectory.step = 1   # Time between trajectory points [s]
trajectory.stop = 4   # Time of final trajectory point [s]
release.start = 2015-09-07T01:00:00  # Time of first release [date]
release.stop = 2015-09-07T02:00:00   # Time of final release [date]
release.step = 3600                  # Time between releases [s]
