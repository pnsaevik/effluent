# Characteristics of the pipe and the effluent flow
pipe.csv.file = "pipe.csv"

# Characteristics of the ambient water masses
ambient.csv.file = "ambient.csv"

# Output options
[output]
csv.file = "out.csv"
trajectory.step = 1   # Output resolution [s]
trajectory.stop = 4   # Time of stagnation [s]
release.step = 3600   # Output frequency [s]
release.stop = 3600   # Stop output after a certain time [s]
